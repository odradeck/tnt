<div class="center hero-unit">
  <h1>오늘의 교훈 내일의 목표</h1>

  <h2>
    인생은 하루 하루 배움의 연속입니다.<br/>
    전체등록교훈 : <%=Lesson.all.count%>
  </h2>
  	<% unless user_signed_in? %><center>
  <%= link_to "당장가입", new_user_registration_path, class: "btn btn-large btn-primary" %>
  <a href="/users/auth/facebook" class="services"><img src="/facebook_64.png" width="45px"></a> 
  </center>
  <%end%>


</div>
  <h4>[최근 등록된 교훈]</h4><br/>
<% @lessons.each do |lesson| %>
 <div class='row-fluid'>
	<div class='span6'>
		<div class="well">
			<p>
				<%= lesson.today %> (<%=lesson.today.date.strftime("%m-%d")%>)
			</p>
		</div>
	</div>
	<div class='span6'>
		<div class="well">
			<p>
				<%= lesson.tomorrow (<%=lesson.tomorrow.date.strftime("%m-%d")%>) %>
			</p>
		</div>
	</div>
</div>
<% end %>